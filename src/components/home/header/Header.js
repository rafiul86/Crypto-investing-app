import React from 'react';
import HeaderMain from './headerMain/HeaderMain';

const Header = () => {
    return (
        <div>
            <HeaderMain />
        </div>
    );
};

export default Header;