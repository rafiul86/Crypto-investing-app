import React from 'react';

const SideBar = () => {
    return (
        <div style={{backgroundColor : '#151371'}}>
            <h1 style={{color : 'white'}}>This is Sidebar</h1>
        </div>
    );
};

export default SideBar;